name: my-workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 11 for x64
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'
        architecture: x64
      
    - name: Build with Maven
      run: mvn clean install
   
   
    - name: uploading build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: my-artifact
        path: target/*.jar
   
   
    - name: Checkout Artifacts branchgit
      run: |
        git config user.name "devops1406-ak"
        git config user.email "kumar.akash.beehyv@gmail.com"
        git checkout -b artifacts-store
        git status